{
  "mcpServers": {
    "archhelp-claude-plugin": {
      "command": "python",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/main.py"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
